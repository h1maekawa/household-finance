// lib/category-colors.ts
//
// カテゴリ別グラフの配色。以前はダッシュボード(9色)と収支ページ(11色)に別々に
// 定義されており、先頭9色は同じで末尾だけ違うという状態だった。色はインデックスで
// 割り当てられるため、カテゴリ数が10を超えると同じカテゴリが画面ごとに別の色になっていた。
//
// 11色版に統一する(多いぶんには色が余るだけで、少ないと衝突するため)。
export const CATEGORY_COLORS = [
  '#1476B3',
  '#1FAE8C',
  '#E2544B',
  '#F0B429',
  '#7C5CFF',
  '#EF7B45',
  '#2F80ED',
  '#6B7280',
  '#9B51E0',
  '#14B8A6',
  '#F97316',
] as const

// 割り当ては従来どおり「金額順に並べたインデックス」で行う(呼び出し側で
// CATEGORY_COLORS[i % CATEGORY_COLORS.length])。順位が変わるとカテゴリの色も
// 変わる仕様だが、これは既存の挙動なのでIAリファクタでは変更しない。
